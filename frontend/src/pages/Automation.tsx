import { Workflow } from 'lucide-react'
import PlaceholderPage from '../components/PlaceholderPage'

export default function Automation() {
  return (
    <PlaceholderPage
      title="Automation"
      description="No-code flow builder — set up triggers and actions to automate your messaging."
      icon={Workflow}
      phase="Phase 2"
    />
  )
}
