import { FileText } from 'lucide-react'
import PlaceholderPage from '../components/PlaceholderPage'

export default function Templates() {
  return (
    <PlaceholderPage
      title="Templates"
      description="Create and manage message templates — Marketing, Utility, and Authentication categories."
      icon={FileText}
      phase="MVP"
    />
  )
}
