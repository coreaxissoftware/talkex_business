import { Megaphone } from 'lucide-react'
import PlaceholderPage from '../components/PlaceholderPage'

export default function Campaigns() {
  return (
    <PlaceholderPage
      title="Campaigns"
      description="9-step campaign builder: name → channel → list → template → personalize → media → schedule → review → launch."
      icon={Megaphone}
      phase="MVP"
    />
  )
}
