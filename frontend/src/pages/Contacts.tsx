import { Users } from 'lucide-react'
import PlaceholderPage from '../components/PlaceholderPage'

export default function Contacts() {
  return (
    <PlaceholderPage
      title="Contacts"
      description="Import, tag, and segment your contacts — shared across all channels."
      icon={Users}
      phase="MVP"
    />
  )
}
