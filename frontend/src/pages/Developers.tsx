import { Code2 } from 'lucide-react'
import PlaceholderPage from '../components/PlaceholderPage'

export default function Developers() {
  return (
    <PlaceholderPage
      title="Developers"
      description="API keys, webhooks, playground, SDKs, logs, and rate limits."
      icon={Code2}
      phase="Phase 2"
    />
  )
}
