import { Wallet } from 'lucide-react'
import PlaceholderPage from '../components/PlaceholderPage'

export default function WalletPage() {
  return (
    <PlaceholderPage
      title="Wallet"
      description="Balance, recharge, transactions, refunds, and coupons."
      icon={Wallet}
      phase="MVP"
    />
  )
}
