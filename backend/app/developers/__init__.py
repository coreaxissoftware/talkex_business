"""Developer Portal: API keys, webhooks, playground, SDKs, logs, rate
limits. API keys mirror the consumer app's `api_keys` table (external/bulk
credential, separate from session auth). Not yet implemented. See
CONTEXT.md.
"""
